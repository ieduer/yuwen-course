# YW executable verification standard

## 2026-09-09 — CText preview mitigation accepted in production

Current Pages **0670bd26-e2d8-494b-bef0-97ebce877ce5**, runtime source **5ed3e1b7262c510de0434d4d690465f04931f7d4** (PR39). Existing v2 executor **1de239210af218ca8b94e726744dd76fee6a31a0** (ops PR40) accepted the deployment after real authentication and post-release telemetry. Immediate rollback is **c66da540-07dc-4f2f-9adf-dbf27fdd251d**, sourcea9d7f654. Artifact1224files/164473235bytes, digestc4026ac38605f1c42b270d87c2aae72423ae625e9bbe04cdf9146ffc03c29566; full config7c6d35e2feb54a7aa1965e9551b253755c315658caaea6b149d9a7753117def6. Logger36279914, bindings, credentials, D1/Queue and native276objects/pointer are unchanged.

Four exact CText references now show existing reviewed1024×640 WebP first screens dated2026-08-11, with explicit not-full-text captions and original URLs, including Liji #n9662. Real owner flow saved3marks and submitted initial reading06:50:34Z; My contained the corresponding non-scoring initialReadingSubmitted and5resourceOpened events. Both source and My UI read back after reload at06:59:58Z. All4compact,4enlarged and4reloaded images decoded, with0CTextproxyrequests. This verifies the actual preview/initial-reading path; screenshots do not restore the publisher's full-text service.

Fixed06:44:10–07:10Z telemetry: private logger78/78 retained ABR1 events,14requests withterminals,9HTTP200/5non-CText403/0CText/0HTTP521. Zone133adaptive aggregate requests,0HTTP5xx/521; sampleintervals1–4, so this is not133raw requests. Prior4storedCText521s are absent from the exercised repaired path. Five other preview403s are existing-policy redirect denials after upstream301/302/307, not raw upstream403. One learning/interactions422 at07:00:24.953Z remains unexplained. Those defects and fullYW/AI/fleethealth remain independently open; do not claim overall completion.

Acceptance authority: `/Users/ylsuen/CF/_meta/reports/operations/.my-architecture-serial7-20260909/yw-521-production-acceptance.json` (SHA25643b04bf8dc33b200020dfbb8bb084323eee4e9a3dddc06cc20c3374578cdd4b4); exact accept output `yw-521-production-accept-output.log`; final telemetry `yw-521-postrelease-freeze.json` supersedes the initial metadata-ID/null interpretation. Journal `yw-ctext-release-control/journal.jsonl` is **accepted**, SHA2564c56af5d391524a0b8b17e1fa7335e3c401ee37a118a061dcfef5612e382145c. Ownercodex-my-architecture-serial7.

Rollback procedure: use clean registered worktrees pinned to the exact YW5ed3e1b and executor1de2392 commits, Node24.18.0, then run `node foundation-yw-pages-leaf-release.mjs rollback --ops-dir <OPS_WORKTREE> --yw-dir <YW_WORKTREE> --executor-sha 1de239210af218ca8b94e726744dd76fee6a31a0` from the opsworktree. The existing v2 rollback explicitly accepts an accepted journal, verifies the exact owned candidate, and restores c66da540 with full config/Web readback. Never replay deploy or edit the immutable journal; preserve all forward student evidence. Later documentation commits are not deployed runtime authority.

Resource location/restore: source and four WebP inputs remain Git-backed in ieduer/yuwen-course; executor inieduer/cf-ops-scripts. Restore into absent manifest-registered worktrees using `git worktree add --detach <ABSENT_YW_WORKTREE> 5ed3e1b7262c510de0434d4d690465f04931f7d4` and equivalent opscommit1de239210af218ca8b94e726744dd76fee6a31a0; verify exactHEAD/clean status before rollback. Existingone-usejournal and sanitizedevidence remain retain_hot under E7, review2026-09-16, until superseding recovery acceptance; review date is not deletion authority. Reproducible taskworktrees/builds remain root-owned for exact cleanup. No new dataset/archive/credential is added. CAPABILITY_FIT=no-new-capability.

The older dated states below are historical where this accepted release supersedes them. `/api/learning/health` drains the outbox and `/api/preview` may issue a CText loginPOST; neither is a read-only health probe.


This file routes the existing eight-point authority in `docs/VERIFICATION.md`.
Current incident20260909-yw-ctext-local-preview is not accepted until its
published artifact and real authenticated lesson/reload evidence are recorded.

1. Source authority: clean published `ieduer/yuwen-course` SHA in the transaction
   receipt, formal `.release/site`; canonical dirty checkout is not deployment
   authority. Verify `git status --short`, source/config/asset hashes.
2. Safe availability: bounded public static root/asset reads and control-plane
   Pages/DNS readback. Never substitute health endpoints that write or a200 for
   product acceptance. CText preview may authenticate upstream.
3. Contracts: `npm run test:resource-previews`, `npm run precontent:check`,
   `npm run verify:study-guide-sources`; exact Node24.18.0. Existing physical
   dependencies are reused; no unconditional install is required. On macOS use
   existing LaunchServices test mode with Google Chrome and a manifest-owned
   profile root; close/remove every test profile.
4. Artifact/deploy: `npm run build:release-site`, `npm run build:artifact-manifest`,
   `npm run check:release-site`, `npm run check:artifact-manifest`, then
   `npm run check:native-content:web-only -- --receipt <ABSOLUTE_CURRENT_RECEIPT>`
   on the clean merged source. The separately reviewed renewed v2 executor owns
   preview/production deploy and rollback; no direct Pages override.
5. Dependency acceptance: preserve APIS/UC/D1/Queue/logger config and unchanged
   native pointer/immutable objects. In a genuinely logged-in Web session open
   lesson1474 through its normal UI, load all four dated local previews, enlarge
   and close them, reload/reopen and prove original links/history remain valid.
   Record actual automatic preview requests and failures without raw personal
   data. No manufactured learning completion or unnecessary AI request.
6. Backup/restore: source and unchanged content use exact Git/artifact hashes;
   Pages rollback preserves D1/Queue and credentials. Existing cloud D1 full
   restore remains a separate unaccepted issue; this repair does not claim it.
7. Rollback: new executor targets Pagesc66da540/sourcea9d7f654; verify the
   canonical deployment, same configuration and full old Web hashes afterward.
   Older e0ffb33c is historical, not the immediate repair preimage.
8. Last verified: see project manual, root evidence and current transaction
   receipt. Tests, artifact, preview, production readback, error trend and real
   authenticated acceptance are separate recorded conclusions.
