# YW executable verification standard

This file routes the existing eight-point authority in `docs/VERIFICATION.md`.
Current incident20260909-yw-ctext-local-preview is not accepted until its
published artifact and real authenticated lesson/reload evidence are recorded.

1. Source authority: clean published `ieduer/yuwen-course` SHA in the transaction
   receipt, formal `.release/site`; canonical dirty checkout is not deployment
   authority. Verify `git status --short`, source/config/asset hashes.
2. Safe availability: bounded public static root/asset reads and control-plane
   Pages/DNS readback. Never substitute health endpoints that write or a200 for
   product acceptance. CText preview may authenticate upstream.
3. Contracts: `npm run test:resource-previews`, `npm run precontent:check`,
   `npm run verify:study-guide-sources`; exact Node24.18.0. Existing physical
   dependencies are reused; no unconditional install is required. On macOS use
   existing LaunchServices test mode with Google Chrome and a manifest-owned
   profile root; close/remove every test profile.
4. Artifact/deploy: `npm run build:release-site`, `npm run build:artifact-manifest`,
   `npm run check:release-site`, `npm run check:artifact-manifest`, then
   `npm run check:native-content:web-only -- --receipt <ABSOLUTE_CURRENT_RECEIPT>`
   on the clean merged source. The separately reviewed renewed v2 executor owns
   preview/production deploy and rollback; no direct Pages override.
5. Dependency acceptance: preserve APIS/UC/D1/Queue/logger config and unchanged
   native pointer/immutable objects. In a genuinely logged-in Web session open
   lesson1474 through its normal UI, load all four dated local previews, enlarge
   and close them, reload/reopen and prove original links/history remain valid.
   Record actual automatic preview requests and failures without raw personal
   data. No manufactured learning completion or unnecessary AI request.
6. Backup/restore: source and unchanged content use exact Git/artifact hashes;
   Pages rollback preserves D1/Queue and credentials. Existing cloud D1 full
   restore remains a separate unaccepted issue; this repair does not claim it.
7. Rollback: new executor targets Pagesc66da540/sourcea9d7f654; verify the
   canonical deployment, same configuration and full old Web hashes afterward.
   Older e0ffb33c is historical, not the immediate repair preimage.
8. Last verified: see project manual, root evidence and current transaction
   receipt. Tests, artifact, preview, production readback, error trend and real
   authenticated acceptance are separate recorded conclusions.
